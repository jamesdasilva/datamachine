var _chai = require('chai');
import AttributeGenerator from './attribute-generator';

describe('AttributeGenerator', function() {
  describe('.genarate()', function() {
    //new AttributeGenerator().run();
    //_chai.expect(AttributeGenerator.getFullYear).to.be.a('function');
  });
});